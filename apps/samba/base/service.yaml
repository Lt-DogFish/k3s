apiVersion: v1
kind: Service
metadata:
  name: samba-service
  namespace: samba
spec:
  type: LoadBalancer
  ports:
  - port: 445
    targetPort: 445
    protocol: TCP
    name: smb
  selector:
    app: samba-server