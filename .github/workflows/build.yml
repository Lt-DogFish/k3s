name: Build and Push Operator to GHCR

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set version
        id: version
        run: |
          echo "version=v${{ github.run_number }}" >> $GITHUB_OUTPUT

      - name: Log into GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and Push Image
        run: |
          docker build \
            -t ghcr.io/lt-dogfish/python-operator:latest \
            -t ghcr.io/lt-dogfish/python-operator:${{ steps.version.outputs.version }} \
            -f ./apps/devices/python-operator/Dockerfile \
            ./apps/devices/python-operator/

      - name: Push image
        run: |
          docker push ghcr.io/lt-dogfish/python-operator:latest
          docker push ghcr.io/lt-dogfish/python-operator:${{ steps.version.outputs.version }}
