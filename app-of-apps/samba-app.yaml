apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: samba-server
  namespace: argocd

spec:
  project: default

  source:
    repoURL: git@github.com:Lt-DogFish/k3s.git
    targetRevision: main
    path: apps/samba/base

  destination:
    server: https://kubernetes.default.svc
    namespace: samba

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
